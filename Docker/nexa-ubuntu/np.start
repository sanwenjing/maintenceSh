#!/bin/sh
if [ -n "$ROOT_PASSWORD" ]; then
  echo "root:$ROOT_PASSWORD" | chpasswd
fi
nohup /usr/sbin/crond>/dev/null 2>&1
cd /opt/ComfyUI && nohup python3 main.py --listen 0.0.0.0 --port 8000 > /tmp/comfyui.log 2>&1 &
nohup /usr/sbin/sshd -D>/dev/null 2>&1
tail -f /etc/hosts
